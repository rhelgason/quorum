{
  "name": "quorum",
  "private": true,
  "version": "0.0.0",
  "description": "A drop-in feedback layer for any app — capture what users want, cluster it, and hand engineering a spec.",
  "license": "MIT",
  "author": "Ryan Helgason",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "node --test 'packages/*/src/**/*.test.ts'",
    "test:watch": "node --test --watch 'packages/*/src/**/*.test.ts'",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-exclude='**/cli.ts' --test-coverage-lines=90 --test-coverage-branches=85 --test-coverage-functions=90 'packages/*/src/**/*.test.ts'",
    "eval": "node packages/eval/src/cli.ts",
    "lint": "npm run lint --workspaces --if-present",
    "size": "npm run size --workspaces --if-present",
    "check": "npm run typecheck && npm run test:coverage"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
